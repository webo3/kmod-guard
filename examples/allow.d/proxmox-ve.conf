# Proxmox VE host: modules loaded on demand when guests start.
# Add the modules for your storages too: nfs nfsv3 nfsv4 (NFS), cifs,
# rbd (Ceph RBD), ceph (CephFS), iscsi_tcp (iSCSI).
path:kernel/drivers/vfio/          # PCI passthrough
path:zfs/
spl
zfs
vhost
vhost_net
tap
tun
macvtap
bridge
br_netfilter
veth                               # LXC
8021q
bonding
vxlan                              # SDN
openvswitch
ifb
nbd

# VM NIC rate limits
sch_htb
sch_ingress
cls_basic
cls_u32
act_police
act_mirred
